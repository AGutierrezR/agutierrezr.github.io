import React from 'react';

import { Nav } from '@components'

const Header = () => (
  <header>
    <div>
      Logo
    </div>
    <div>
      <Nav />
    </div>
  </header>
);

export default Header;